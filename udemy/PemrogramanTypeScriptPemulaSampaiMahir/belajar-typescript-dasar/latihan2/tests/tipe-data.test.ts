describe('Data Type', () => {
    it('should must declare', () => {
        let name: string = "Gary Sanderson";
        let balance: number = 100000;
        let isVip: boolean = true;

        console.info(name);
        console.info(balance);
        console.info(isVip);

        // name = 100;
        // balance = "Roach";
        // isVip = 1;
    });
});